// import React, { useState } from 'react';
import Neww from './components/neww';

const App = () => {
  // let arr = ["vipul", "yash", "bhavya"]
  // const [num, setNum] = useState(0)
  return (
    <>
      {/* <h1>{arr[num]}</h1>

      <button onClick={() => {
        if (num < arr.length - 1) {
          setNum(num + 1)
        }
      }}>Change user</button> */}




      <Neww />
    </>
  );
}

export default App;